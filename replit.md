# shourov-fca

## Overview
A Facebook Chat API library for Node.js with session management, anti-ban mechanisms, and bot development features.

## Recent Changes
- 2026-02-09: Imported project into Replit environment, installed Node.js 20 and npm dependencies, fixed class export mismatch in `src/api/api.js`.

## Project Architecture
- **Runtime**: Node.js 20
- **Entry point**: `index.js` — exports `login` and `ShourovAPI`
- **Source**: `src/` directory with `api/`, `core/`, and `utils/` subdirectories
- **Type**: npm library (not a web server)

## User Preferences
- None recorded yet.
